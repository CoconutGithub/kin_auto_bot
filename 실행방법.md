# 실행 방법

## 1. 요구 사항
- Node.js 설치
- npm 사용 가능 환경
- Google Gemini API Key 준비
- 네이버 로그인 가능 계정 준비

## 2. 의존성 설치
프로젝트 루트에서 아래 명령을 실행합니다.

```bash
npm install
```

## 3. 환경 변수 설정
루트의 `.env` 파일에 아래 값을 설정합니다.

```env
GOOGLE_API_KEY=여기에_구글_API_KEY
NAVER_ID=네이버_아이디
NAVER_PW=네이버_비밀번호
HEADLESS=false
```

설명:
- `GOOGLE_API_KEY`: 필수입니다.
- `NAVER_ID`, `NAVER_PW`: 자동 입력에 사용됩니다. 없어도 브라우저에서 수동 로그인할 수 있습니다.
- `HEADLESS=false`: 브라우저 화면을 보면서 로그인하려면 `false`를 권장합니다.

## 4. 프로젝트 설정
`config/settings.js`에서 아래 항목을 필요에 맞게 수정합니다.
- `projects`: 모니터링할 프로젝트와 키워드 목록
- `searchInterval`: 검색 주기
- `aiModel`: 사용할 Gemini 모델
- `maxAnswerLength`: 답변 최대 길이
- `privateId`: 답변 작성 시 아이디 비공개 여부

## 5. 실행
프로젝트 루트에서 아래 명령을 실행합니다.

```bash
node index.js
```

실행 후 동작:
- Puppeteer 브라우저가 실행됩니다.
- 네이버 로그인을 진행합니다.
- `config/settings.js`에 있는 키워드로 지식인 질문을 주기적으로 검색합니다.
- 답변 후보가 나오면 터미널에서 답변 생성 여부와 등록 여부를 확인받습니다.

## 6. 보조 테스트 명령
AI 연결 테스트:

```bash
node src/test-ai.js
```

모델 확인용 스크립트:

```bash
node src/list-models.js
```

## 7. 참고 파일
- 실행 진입점: `index.js`
- 메인 봇 로직: `src/bot.js`
- 설정 파일: `config/settings.js`
- 답변 기록: `data/history.json`
- 브라우저 세션 데이터: `user_data/`
